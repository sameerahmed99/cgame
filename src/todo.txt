1. Triangle clipping, performance drops when triangles become big, especially when they're bigger than the resolution

2. Move game core features to a common file (such as the main loop, fixed loop, cg_create_color_from_channels function etc) so that we have those functions ready to use in all test games
